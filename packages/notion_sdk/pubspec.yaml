name: notion_sdk
description: A wrapper for Notion's SDK made for the LiteN app.
version: 0.1.2
homepage: https://liten.app

environment:
  sdk: ">=2.12.0 <3.0.0"

dev_dependencies:
  build_runner: ^2.4.13
  json_serializable: ^6.8.0
  pedantic: ^1.9.0
  test: ^1.14.4

dependencies:
  enum_to_string: ^2.0.1
  http: ^1.2.2
  json_annotation: ^4.9.0
