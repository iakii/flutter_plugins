// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.5.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`

enum BitTorrentFileMode {
  single,
  multi,
  ;
}

class BittorrentStatus {
  final List<List<String>> announceList;
  final String? comment;
  final DateTime? creationDate;
  final BitTorrentFileMode? mode;

  const BittorrentStatus({
    required this.announceList,
    this.comment,
    this.creationDate,
    this.mode,
  });

  @override
  int get hashCode =>
      announceList.hashCode ^
      comment.hashCode ^
      creationDate.hashCode ^
      mode.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is BittorrentStatus &&
          runtimeType == other.runtimeType &&
          announceList == other.announceList &&
          comment == other.comment &&
          creationDate == other.creationDate &&
          mode == other.mode;
}

class File {
  final BigInt index;
  final String path;
  final BigInt length;
  final BigInt completedLength;
  final bool selected;
  final List<Uri> uris;

  const File({
    required this.index,
    required this.path,
    required this.length,
    required this.completedLength,
    required this.selected,
    required this.uris,
  });

  @override
  int get hashCode =>
      index.hashCode ^
      path.hashCode ^
      length.hashCode ^
      completedLength.hashCode ^
      selected.hashCode ^
      uris.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is File &&
          runtimeType == other.runtimeType &&
          index == other.index &&
          path == other.path &&
          length == other.length &&
          completedLength == other.completedLength &&
          selected == other.selected &&
          uris == other.uris;
}

class GetServersResult {
  final int index;
  final List<Server> servers;

  const GetServersResult({
    required this.index,
    required this.servers,
  });

  @override
  int get hashCode => index.hashCode ^ servers.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is GetServersResult &&
          runtimeType == other.runtimeType &&
          index == other.index &&
          servers == other.servers;
}

class GlobalStat {
  final BigInt downloadSpeed;
  final BigInt uploadSpeed;
  final int numActive;
  final int numWaiting;
  final int numStopped;
  final int numStoppedTotal;

  const GlobalStat({
    required this.downloadSpeed,
    required this.uploadSpeed,
    required this.numActive,
    required this.numWaiting,
    required this.numStopped,
    required this.numStoppedTotal,
  });

  @override
  int get hashCode =>
      downloadSpeed.hashCode ^
      uploadSpeed.hashCode ^
      numActive.hashCode ^
      numWaiting.hashCode ^
      numStopped.hashCode ^
      numStoppedTotal.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is GlobalStat &&
          runtimeType == other.runtimeType &&
          downloadSpeed == other.downloadSpeed &&
          uploadSpeed == other.uploadSpeed &&
          numActive == other.numActive &&
          numWaiting == other.numWaiting &&
          numStopped == other.numStopped &&
          numStoppedTotal == other.numStoppedTotal;
}

class Peer {
  final bool amChoking;
  final String bitfield;
  final BigInt downloadSpeed;
  final String ip;
  final bool peerChoking;
  final String peerId;
  final int port;
  final bool seeder;
  final BigInt uploadSpeed;

  const Peer({
    required this.amChoking,
    required this.bitfield,
    required this.downloadSpeed,
    required this.ip,
    required this.peerChoking,
    required this.peerId,
    required this.port,
    required this.seeder,
    required this.uploadSpeed,
  });

  @override
  int get hashCode =>
      amChoking.hashCode ^
      bitfield.hashCode ^
      downloadSpeed.hashCode ^
      ip.hashCode ^
      peerChoking.hashCode ^
      peerId.hashCode ^
      port.hashCode ^
      seeder.hashCode ^
      uploadSpeed.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is Peer &&
          runtimeType == other.runtimeType &&
          amChoking == other.amChoking &&
          bitfield == other.bitfield &&
          downloadSpeed == other.downloadSpeed &&
          ip == other.ip &&
          peerChoking == other.peerChoking &&
          peerId == other.peerId &&
          port == other.port &&
          seeder == other.seeder &&
          uploadSpeed == other.uploadSpeed;
}

class Server {
  final String uri;
  final String currentUri;
  final BigInt downloadSpeed;

  const Server({
    required this.uri,
    required this.currentUri,
    required this.downloadSpeed,
  });

  @override
  int get hashCode =>
      uri.hashCode ^ currentUri.hashCode ^ downloadSpeed.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is Server &&
          runtimeType == other.runtimeType &&
          uri == other.uri &&
          currentUri == other.currentUri &&
          downloadSpeed == other.downloadSpeed;
}

class SessionInfo {
  final String sessionId;

  const SessionInfo({
    required this.sessionId,
  });

  @override
  int get hashCode => sessionId.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is SessionInfo &&
          runtimeType == other.runtimeType &&
          sessionId == other.sessionId;
}

/// Full status of a task.
///
/// <https://aria2.github.io/manual/en/html/aria2c.html#aria2.tellStatus>
class Status {
  /// GID of the download.
  final String gid;
  final TaskStatus status;
  final BigInt totalLength;
  final BigInt completedLength;
  final BigInt uploadLength;

  /// Hexadecimal representation of the download progress.
  ///
  /// The highest bit corresponds to the piece at index 0.
  ///
  /// Any set bits indicate loaded pieces, while
  /// unset bits indicate not yet loaded and/or missing pieces.
  ///
  /// Any overflow bits at the end are set to zero.
  ///
  /// When the download was not started yet,
  /// this key will not be included in the response.
  final String? bitfield;
  final BigInt downloadSpeed;
  final BigInt uploadSpeed;

  /// InfoHash. BitTorrent only
  final String? infoHash;
  final BigInt? numSeeders;

  /// true if the local endpoint is a seeder. Otherwise false. BitTorrent only.
  final bool? seeder;
  final BigInt pieceLength;
  final BigInt numPieces;
  final BigInt connections;
  final String? errorCode;
  final String? errorMessage;

  /// List of GIDs which are generated as the result of this download.
  ///
  /// For example, when aria2 downloads a Metalink file,
  /// it generates downloads described in the Metalink (see the --follow-metalink option).
  ///
  /// This value is useful to track auto-generated downloads.
  ///
  /// If there are no such downloads, this key will not be included in the response.
  final List<String>? followedBy;

  /// The reverse link for followedBy.
  ///
  /// A download included in followedBy has this object's GID in its following value.
  final String? following;

  /// GID of a parent download.
  ///
  /// Some downloads are a part of another download.
  ///
  /// For example, if a file in a Metalink has BitTorrent resources,
  /// the downloads of ".torrent" files are parts of that parent.
  ///
  /// If this download has no parent, this key will not be included in the response.
  final String? belongsTo;
  final String dir;
  final List<File> files;
  final BittorrentStatus? bittorrent;

  /// The number of verified number of bytes while the files are being hash checked.
  ///
  /// This key exists only when this download is being hash checked.
  final BigInt? verifiedLength;

  /// `true` if this download is waiting for the hash check in a queue.
  ///
  /// This key exists only when this download is in the queue.
  final bool? verifyIntegrityPending;

  const Status({
    required this.gid,
    required this.status,
    required this.totalLength,
    required this.completedLength,
    required this.uploadLength,
    this.bitfield,
    required this.downloadSpeed,
    required this.uploadSpeed,
    this.infoHash,
    this.numSeeders,
    this.seeder,
    required this.pieceLength,
    required this.numPieces,
    required this.connections,
    this.errorCode,
    this.errorMessage,
    this.followedBy,
    this.following,
    this.belongsTo,
    required this.dir,
    required this.files,
    this.bittorrent,
    this.verifiedLength,
    this.verifyIntegrityPending,
  });

  @override
  int get hashCode =>
      gid.hashCode ^
      status.hashCode ^
      totalLength.hashCode ^
      completedLength.hashCode ^
      uploadLength.hashCode ^
      bitfield.hashCode ^
      downloadSpeed.hashCode ^
      uploadSpeed.hashCode ^
      infoHash.hashCode ^
      numSeeders.hashCode ^
      seeder.hashCode ^
      pieceLength.hashCode ^
      numPieces.hashCode ^
      connections.hashCode ^
      errorCode.hashCode ^
      errorMessage.hashCode ^
      followedBy.hashCode ^
      following.hashCode ^
      belongsTo.hashCode ^
      dir.hashCode ^
      files.hashCode ^
      bittorrent.hashCode ^
      verifiedLength.hashCode ^
      verifyIntegrityPending.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is Status &&
          runtimeType == other.runtimeType &&
          gid == other.gid &&
          status == other.status &&
          totalLength == other.totalLength &&
          completedLength == other.completedLength &&
          uploadLength == other.uploadLength &&
          bitfield == other.bitfield &&
          downloadSpeed == other.downloadSpeed &&
          uploadSpeed == other.uploadSpeed &&
          infoHash == other.infoHash &&
          numSeeders == other.numSeeders &&
          seeder == other.seeder &&
          pieceLength == other.pieceLength &&
          numPieces == other.numPieces &&
          connections == other.connections &&
          errorCode == other.errorCode &&
          errorMessage == other.errorMessage &&
          followedBy == other.followedBy &&
          following == other.following &&
          belongsTo == other.belongsTo &&
          dir == other.dir &&
          files == other.files &&
          bittorrent == other.bittorrent &&
          verifiedLength == other.verifiedLength &&
          verifyIntegrityPending == other.verifyIntegrityPending;
}

/// Task status returned by `aria2.tellStatus`.
///
/// `Active` for currently downloading/seeding downloads.
///
/// `Waiting` for downloads in the queue; download is not started.
///
/// `Paused` for paused downloads.
///
/// `Error` for downloads that were stopped because of error.
///
/// `Complete` for stopped and completed downloads.
///
/// `Removed` for the downloads removed by user.
///
/// <https://aria2.github.io/manual/en/html/aria2c.html#aria2.tellStatus>
enum TaskStatus {
  active,
  waiting,
  paused,
  error,
  complete,
  removed,
  ;
}

class Uri {
  final UriStatus status;
  final String uri;

  const Uri({
    required this.status,
    required this.uri,
  });

  @override
  int get hashCode => status.hashCode ^ uri.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is Uri &&
          runtimeType == other.runtimeType &&
          status == other.status &&
          uri == other.uri;
}

enum UriStatus {
  used,
  waiting,
  ;
}

class Version {
  final List<String> enabledFeatures;
  final String version;

  const Version({
    required this.enabledFeatures,
    required this.version,
  });

  @override
  int get hashCode => enabledFeatures.hashCode ^ version.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is Version &&
          runtimeType == other.runtimeType &&
          enabledFeatures == other.enabledFeatures &&
          version == other.version;
}
